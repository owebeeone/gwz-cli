# gwz 1.0.17 release documentation: remediation plan (round 1)

Object: gwz-cli `ad095e9` (root `4ff2834`, gwz-core `95138c7d`, gwz-py `68de94cc`).
Reviews: `GwzRelease1017Docs-ReviewSurface.md` (NO-GO, P2-1..P2-3, P3-1..P3-6)
and `GwzRelease1017Docs-ReviewConsistency.md` (NO-GO, P2-1..P2-3, P3-1..P3-5).
Both pre-committed to GO on a revision resolving their P2s. Blind
convergence: Surface P2-1 and Consistency P2-3 (a dry-run fetch row is
indistinguishable from a contacted no-op); Surface P3-1 and Consistency P3-1
(`--force <categories>`).

One patch. IDs are prefixed S (Surface) and C (Consistency).

| Finding | Disposition | Closure test |
| --- | --- | --- |
| S-P2-1, C-P2-3 | Fix in code, not only text: `gwz fetch` is unreleased so the token can still change. Add `planned` to the `FetchResult` enum in `gwz-core/protocol/gwz.taut.py` (wire 4), regenerate gwz-core and gwz-py, make `FetchTarget::planned_row` return `Planned` for a repository that would be contacted, render it as `would contact <remote>` in gwz-cli and in gwz-py's renderer, and document it: `fetch_long.rs` (and add `gwz --dry-run fetch` to its Examples), `docs/commands/fetch.md` (carve-out in "What it does not do", `would contact` in Output), `docs/MachineOutput.md` (`Planned` means not contacted; `Unchanged` still means contacted), `docs/Releases.md`, `skills/gwz/SKILL.md`, `docs/CLI.md` regenerated. | `gwz --dry-run fetch` prints `would contact origin` and `--json` prints `"result":"Planned"` on a repository whose upstream is one commit ahead; neither string appears in a live fetch; gwz-cli g15 dry-run test and gwz-py parity test assert the new value; `tests/protocol.rs::generated_protocol_is_current` passes. |
| S-P2-2 | Text. `docs/ClaudeCode.md` L162-167, L193-195, L197-204: a session that writes outside git, including `.claude/settings.local.json` and `.claude/.cc-writes/`, still produces a `changed copy` or `unique to the lane` refusal on 1.0.17; name the retirement section as the remedy; qualify "you do nothing" to a lane the session did not write outside git. | The page's promise matches Surface's Case A and Case B outcomes. |
| S-P2-3 | Text. Quote the refusal a 1.0.13 gwz prints, derived from its source (`v1.0.13:crates/family-store/src/format.rs:37-42`): ``gwz: ManifestInvalid: … .gwz/local-family.yml is malformed: `schema: gwz.local-family/v2` is not `gwz.local-family/v1` (format version 1); this file is not in a format this store reads``, and say plainly it names no version and the remedy is to upgrade every gwz on that workspace to 1.0.14 or later. Same at `docs/ClaudeCode.md:31-32`. Say the quote comes from the 1.0.13 source, not a run. | `grep -n 'names the minimum' docs/Releases.md` returns nothing; the quoted line matches the 1.0.13 `wrong_schema` format string. |
| C-P2-1 | Text. Sweep `MissingRemote`/`missing_remote` at `docs/LocalClones.md:529-531`, `docs/commands/pull.md:68-69`, `docs/commands/push.md:43-44` to `GitCommandFailed: remote '<name>' does not exist`. | `grep -rn "MissingRemote\|missing_remote" docs/` returns nothing. |
| C-P2-2 | Text. `docs/ClaudeCode.md:216-226`: the default merge selection already carries the lane's root commits; show the bare `gwz merge --remote NAME` the hook prints; `--target @all` only when a narrower selection was used. Apply the same correction to `skills/gwz/SKILL.md` where it says to include `@all` so root history travels. | The sentence agrees with `docs/commands/merge.md:15-17` and `src/hook/remove.rs`'s remedy string. |
| S-P3-1, C-P3-1 | Text. `--force <hazard,...>` at the three sites. | `grep -rn 'force <categories>' docs/` returns nothing. |
| S-P3-2 | Text. Restate the Subagents bullet: the prohibition stands because a subagent that writes outside git still leaves a refused lane and the hook cannot tell a subagent from a session; no "until" clause. | No sentence gates on "until an integrated lane disposes in one command". |
| S-P3-3 | Text. `docs/ClaudeCode.md:81-87` and the `setup` long help in `src/hook/setup.rs` (or wherever the sentence lives): `--wait-secs` is emitted only when the estimate raises the wait above the built-in 300 s default; absence means the default. Regenerate CLI.md if the help changes. | A small-workspace `setup --project` block is fully accounted for by the page. |
| S-P3-4 | Dispute in part, defer the code. The `Ref { name: … }` rendering is gwz-core's released dispose message and changing it is not a docs change; file it as a follow-up in gwz-dev `dev-docs/GwzLaneIssues.md` is out of this lane's scope, so record it in the acceptance note. Fix the prose only: do not claim the report prints "the paths or object ids it holds" where a protected root prints a ref. | The prose at `docs/LocalClones.md` around L363-445 does not overclaim; the sample stays as the binary prints it. |
| S-P3-5 | Text. Carve-out in `docs/commands/fetch.md:27-29`; Examples parity between `fetch_long.rs` and `fetch.md` (covered by S-P2-1's edits). | `gwz fetch --help` Examples and `fetch.md` Examples list the same invocations. |
| S-P3-6 | Text. L64: recommend `--project --local` (personal, uncommitted) unless the team has decided to commit the hooks. Replace every `S<n>.<m>` identifier on the page with a plain statement of what is unverified; keep the Unmeasured markers; reword the status banner without plan-step ids. | `grep -n 'S[0-9]\.[0-9]' docs/ClaudeCode.md` returns nothing; L64 names a placement. |
| C-P3-2 | Text. Add `new <after>` and `updated` to the row lists in `docs/Releases.md` and `docs/commands/fetch.md`. | Every arm of `movement()` in `src/append_branch_summary/fetch.rs` is listed. |
| C-P3-3 | Text. Regenerate the `gwz fetch` sample from a real run in a throwaway workspace with at least two members, in both files; paths may be shortened to the page's convention but column padding must be the renderer's. | The block re-pads to itself under the renderer's format. |
| C-P3-4 | Text. Fix the Compatibility Notes lead: drop the local-clone clause, widen the release range to "0.10 to 1.0". | Every noun phrase in the lead names a bullet below it. |
| C-P3-5 | Text plus record. State the two observations on this workspace as observations, not a range: 123 s (7 s to the lane directory, 106 s tree copy, 10 s record and index; 2026-09-18, nothing else running) and 146 s (`gwz local clone` by hand, same day, one other lane present). Add the 146 s observation to U1 in `dev-docs/GwzClaudeIntegrationPlan.md` so both numbers are traceable. | Each number on the page appears in the plan's U1 note. |

Re-verdict: the same two reviewers, focused on their own findings, on the
revised tuple.
