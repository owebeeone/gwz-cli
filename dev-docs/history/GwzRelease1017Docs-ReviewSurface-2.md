# GwzRelease1017Docs Surface review — round 2

Tuple verified at start: root `1552ca0242ba3d17268fa220219d9f08c875ba02`, gwz-cli `fe0f1d2302d7f8c3291c49ca70594ba5e83de06c`, gwz-core `8f798b5cb47072208cc88c9e198d965c73462762`, gwz-py `fc42c8ab2dff5cde376de28f79b189690bbbf497`, `git -C gwz-cli status --porcelain` empty, binary `/Users/owebeeone/limbo/gwz-dev-docs-release/target/release/gwz` reporting `gwz 0.2.0-dev`, mtime 2026-09-18 20:14 (after the round-1 binary), free space 8.5 GB ; at end: identical, working tree still clean.

**Binary freshness.** Not stale. `gwz-core` `8f798b5c` ("Report a dry-run fetch row as Planned, never Unchanged") is the only core commit in the range and its effect is present in the binary: `--dry-run --json fetch` returns `"result": "Planned"`, a value that did not exist at the round-1 tuple. The `gwz-cli` help changes in `src/fetch_long.rs` and `src/hook_help.rs` are also present in `--help` output. `--version` still prints `0.2.0-dev` at both tuples, which is the lane's own version string and not an indicator either way.

Peer-blind note: I read `dev-docs/GwzRelease1017Docs-RemPlan.md` (authorised round-2 input, and it summarises the other axis's findings in its own words). I did not open `dev-docs/GwzRelease1017Docs-ReviewConsistency.md`.

## Verdict: GO

All three round-1 P2s and all six P3s are closed. No new findings at P0–P3. This discharges my round-1 pre-commit ("I pre-commit to GO on a revision that resolves P2-1, P2-2 and P2-3 as specified") — and the revision went further, closing every P3 as well.

## Prior-finding closure table

| ID | Status | Evidence at the round-2 tuple |
| --- | --- | --- |
| **P2-1** — dry-run fetch prints `no change` / `Unchanged` when the upstream moved | **Closed** | Fixed in code, not only text, as the plan chose. Fresh throwaway, upstream one commit ahead of the tracking ref: `gwz --dry-run fetch` → `status: Noop` / `@root  .  would contact origin`; `gwz --dry-run --json fetch` → `"result": "Planned"`, `members[0].status = "Planned"`, all of `before`/`after`/`ahead`/`behind`/`upstream` null. Paired single-run checks on the same workspace separated all three cases cleanly: a real move → `b728294..d5a7dec` / `"result":"Updated"`; a genuine no-op → `no change  (origin/main, +0 -2)` / `"result":"Unchanged"` with `before == after`; a dry run → `would contact origin` / `Planned`. No live fetch produced `Planned`; no dry run produced `Unchanged` or `Updated`. The `no upstream` carve-out claimed by `docs/MachineOutput.md` also holds: in a workspace with no remote, `--dry-run fetch` → `no upstream` / `"result":"NoUpstream"`, `members[0].status = "Noop"`. Documented at `docs/commands/fetch.md:34-81` (new `### A dry run contacts nothing` section, reached by the in-page anchor from the carve-out bullet), `docs/MachineOutput.md` (`Planned` entry plus a `### Telling a dry run apart` subsection), `docs/Releases.md`, `src/fetch_long.rs:32-36`, `skills/gwz/SKILL.md`, and `docs/CLI.md` regenerated. |
| **P2-2** — ClaudeCode.md promised hands-off retirement; `.claude` state still refuses | **Closed** | `docs/ClaudeCode.md` now qualifies the promise to "a lane session **that wrote nothing outside git**" and adds a dedicated paragraph: "**A session that writes outside git is still a refused removal on 1.0.17.** ... `.claude/settings.local.json` and `.claude/.cc-writes/` ... A lane that inherited those files by the copy and whose session then edited them refuses them as `changed copy`; a lane whose session created them refuses them as `unique to the lane`. Neither is a leftover from an older release", naming the `--force` branch as the applicable remedy "because nothing in `.claude/` is work a merge can carry". The `--max-lanes` bullet (L186-194) carries the matching correction. Both counterexamples re-run and now match the page exactly: Case A (inherited, then edited) → `changed copy 2: ... (.claude/.cc-writes/), ... (.claude/settings.local.json); unique to the lane 0`; Case B (created in the lane) → `unique to the lane 2: ...; changed copy 0`. Both offer `gwz local dispose <name> --force dirty`. |
| **P2-3** — Releases.md claimed the older gwz's refusal "names the minimum version" | **Closed** | `grep -n 'names the minimum' docs/Releases.md` → nothing. Replaced by "its refusal names no gwz version at all", the verbatim 1.0.13 line, an explicit provenance note ("derived from the 1.0.13 source ... not from a run on this machine"), and "It calls the file malformed, which it is not, and offers no remedy; the remedy is to upgrade." `docs/ClaudeCode.md` Prerequisites carries the same correction, adding "It reads like a corrupt file and is not". I verified the quote's provenance directly: `git -C gwz-core show v1.0.13:crates/family-store/src/format.rs` contains `wrong_schema` formatting exactly `` `schema: {found}` is not `{expected}` (format version {version}); this file is not in a format this store reads `` — the quoted string is real and names no gwz version. |
| **P3-1** — `--force <categories>` misnamed the waiver vocabulary | **Closed** | `grep -rn 'force <categories>' docs/` → nothing. All three sites (`docs/Releases.md:113`, `docs/LocalClones.md:383`, `docs/commands/local.md:307`) now read `--force <hazard,...>`, matching `gwz local dispose -h`'s usage line and the other five pre-existing uses in the docs. Reproduced refusals still print the hazard vocabulary: `--force dirty`, `--force unpreserved-history`. |
| **P3-2** — Subagents bullet keyed to a condition this release satisfies | **Closed** | `grep -n 'until an integrated lane disposes in one command' docs/ClaudeCode.md` → nothing. Rewritten to "Do not use subagent worktree isolation in a GWZ workspace. The prohibition is not waiting on a release. A subagent that writes outside git leaves a lane the remove hook cannot retire, exactly as a session does, and nothing in the hook's input distinguishes a subagent from a session..." — the reason is now the one my P2-2 evidence supports, with no release gate. `skills/gwz/SKILL.md` carries the same rewrite. |
| **P3-3** — `setup` documented as computing `--wait-secs`, emitted none | **Closed** | Both surfaces fixed. `gwz hook claude-code setup --help` now reads "`--wait-secs` is written onto a handler only when the estimate raises the wait above the compiled-in 300 s, so a small workspace's block carries no `--wait-secs` at all: its absence means the 300 s default stands, not that no estimate ran." `docs/ClaudeCode.md:96-101` says the same and adds "the computed timeouts are still there to read". Observed block in the throwaway: `timeout` 361 (create) and 360 (remove), no `--wait-secs` — now fully accounted for by the page with no reader inference. `docs/CLI.md` regenerated to match. |
| **P3-4** — blessed refusal sample contains Rust `{:?}` struct syntax | **Closed as prose; code deferred and recorded** | The plan disputed the code half (the rendering is gwz-core's released dispose message, outside this lane) and fixed the prose, which is what my round-1 required-correction offered as the alternative. `grep -n 'paths or object ids'` → nothing in `docs/`. All three sites now read "with a description of what it holds ... Most entries are paths; a protected root ... is described by its object id and the head or ref that reaches it, in the internal form the sample above shows, **which is not a path you can pass to `git`**." The sample still matches the binary: my re-run printed `Ref { name: "refs/heads/main" } a43d9b99...`, identical in form to `docs/LocalClones.md`'s sample. The overclaim is gone and the reader is warned. The follow-up on the rendering itself remains outstanding in gwz-core; I note below that I could not confirm it was filed. |
| **P3-5** — `docs/commands/fetch.md` lacked the `--dry-run` carve-out and Examples parity | **Closed** | The bullet now reads "**It never skips the network, with one exception: `--dry-run`.**" and links to the new `### A dry run contacts nothing` section in the same page (anchor `#a-dry-run-contacts-nothing`, heading present). Examples parity is exact: `gwz fetch --help` lists `gwz fetch`, `gwz --no-target @root fetch`, `gwz --member mem_app fetch`, `gwz --remote upstream fetch`, `gwz --json fetch`, `gwz --dry-run fetch`; `docs/commands/fetch.md`'s Examples list the same six invocations in the same order. The Output section now enumerates `would contact <remote>` alongside the live row kinds. |
| **P3-6** — draft banner, plan-step identifiers, unfinished sentence | **Closed** | `grep -no 'S[0-9]\.[0-9]' docs/ClaudeCode.md` → nothing; every `S<n>.<m>` is replaced by a plain statement ("no probe has looked"). The banner no longer cites plan steps: "Status: draft. Paragraphs marked **Unmeasured** describe intended behaviour that no probe on this workspace has yet confirmed ... Everything else on this page was checked against the binary." L64's dangling "One is the recommendation" is now L70-75: "`--project --local` is the recommendation: the hooks are a personal choice about how your sessions get their working copy, and `settings.local.json` is not committed ... Use `--project` instead only where the team has decided to commit the hooks for everyone, and `--user` where you want them on every GWZ workspace on the machine." |

## New findings

**None at P0–P3.**

I re-attacked the changed surfaces looking for regressions and found none:

- **`docs/CLI.md` is a faithful regeneration at the new tuple.** Extracted the `gwz fetch`, `gwz local dispose` and `gwz hook claude-code setup` blocks and diffed each against the built binary's `--help` (trailing whitespace normalised): all three **MATCH** exactly, including the new `--dry-run` and `--wait-secs` paragraphs. At round 1 these differed only by generator whitespace; they are now byte-identical under that normalisation.
- **The instruction I was given changed in walkthrough 3 and I re-tested it.** `docs/ClaudeCode.md` and `skills/gwz/SKILL.md` now tell the user to run the bare `gwz merge --remote <name>` rather than `gwz --target @all merge --remote <name>`. On a lane carrying a root-only commit, the bare form fast-forwarded the root (`status: Ok`, `participants: total 1; fast-forwarded 1`) and `gwz local dispose M --wait 30` then succeeded with **no waiver**. The page's justification — "The default selection is the workspace root plus every active member, so the bare command already carries the lane's root commits" — holds on the case I could build. Its `--target @all` caveat ("only to restore that default after a narrower selection") is stated, not left to inference.
- **The dispose surface is unchanged and still matches its docs.** Regenerable-only lane (`target/.rustc_info.json`, `debug/deps/*.dylib`, `debug/.fingerprint`) disposed clean with no waiver. Protected-root refusal printed the four categories in documented order with the empty ones included, offering `gwz local dispose P --force unpreserved-history`. Following that command with one waiver too many printed `forced past: unpreserved-history; unused waiver: dirty`, matching `docs/commands/local.md`'s sample form. `unchanged copy 2` entries (the inherited `.claude` files) were reported and did **not** refuse, confirming the category table's "Refuses: no" column.
- **The new fetch samples re-pad to themselves.** I checked the regenerated live and dry-run blocks in `docs/Releases.md` and `docs/commands/fetch.md` for internal column consistency: member-id column 11 chars (longest `mem_local` + 2), path column 10 (longest `gwz-core` + 2), status column 18 (longest `90ef552..330a174` + 2), with the dry-run block using the same first two widths. Self-consistent under a max-width-plus-two-gutter renderer.
- **No collateral damage from the `MissingRemote` sweep.** `docs/LocalClones.md`, `docs/commands/local.md`, `docs/commands/pull.md` and `docs/commands/push.md` now say `GitCommandFailed: remote '<name>' does not exist`, which is what I observed at round 1 from `gwz pull --remote NOSUCH` and `gwz push --remote NOSUCH`, and re-confirmed is still the message.

## What I could not test and why

Unchanged from round 1 except where noted.

- **A real pre-1.0.14 binary.** Still none on this machine (`~/.cargo/bin/gwz` is 1.0.16). P2-3's remedy no longer depends on one: the page now quotes the 1.0.13 format string and labels it as source-derived rather than observed, and I verified that format string directly in `gwz-core` at tag `v1.0.13`. Reading those six lines is the only code I read beyond my axis's two help-string files, and I read them solely to check a quotation.
- **`gwz fetch` while a coordinated merge is open.** Still asserted in three places and still untested; I could not cheaply construct a stuck open merge in a single-repository throwaway.
- **The hooks driven by Claude Code itself.** I exercised `setup` in all its forms (`--project`, `--project --local --write`, re-write idempotence, `--remove`, remove-when-absent, `--write --remove` mutual exclusion) but not `worktree-create` / `worktree-remove` under a live session. The banner's new claim that "everything else on this page was checked against the binary" covers material I cannot reach — the two-minute silent `WorktreeCreate`, the `Error creating worktree: ...` wrapping, the differing-handler warning — so I can neither confirm nor falsify it.
- **The multi-member case.** My throwaway had only `@root` throughout both rounds. Every fetch row, dispose refusal and merge I compared is a single-repository row; the `mem_*` sample rows, `--partial`, and a dispose refusal spread across several members remain untested, as does the bare-merge root-commit behaviour when members are also present.
- **Whether the deferred `Ref { name: … }` rendering was actually filed as a gwz-core follow-up.** The remediation plan says to "record it in the acceptance note"; I found no acceptance note at this tuple and did not go looking outside the lane. The docs-side remedy stands on its own, so this does not affect the verdict, but the code follow-up's existence is unverified by me.
- **`--max-lanes`, `--min-free-gb`, the free-space estimate, and Windows.** Not exercised; unchanged from round 1.
