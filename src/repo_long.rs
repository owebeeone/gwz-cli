
pub(crate) const REPO_LONG: &str = "\
Manage repository members inside a workspace.

Repository commands bring member repositories into a workspace and manage them.
This group exposes `add` (register an existing repository as a member) and
`create` (initialize a new member from scratch); use top-level commands such as
`gwz status`, `gwz pull`, and `gwz push` for workspace-wide operations.";
